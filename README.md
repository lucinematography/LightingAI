# LIGHTING AI Android

GitHub-ready Android project.

## Build APK
1. Open **Actions**.
2. Open **Build Android APK**.
3. Tap **Run workflow**.
4. When the run finishes, open it and download artifact **LightingAI-APK**.
5. Extract `app-debug.apk` on Android and install it.
