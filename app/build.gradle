plugins { id 'com.android.application' }

android { namespace 'com.lightingai.app'; compileSdk 35
    defaultConfig { applicationId 'com.lightingai.app'; minSdk 26; targetSdk 35; versionCode 8; versionName '1.7' }

    buildTypes {
        debug {
            applicationIdSuffix '.review8'
            versionNameSuffix '-review8'
        }
    }
}
